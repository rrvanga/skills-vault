# OpenCode Go subscription — quotas & usage tracking (verified 2026-08-11)

Provider: opencode-go, base URL `https://opencode.ai/zen/go/v1`, key `OPENCODE_GO_API_KEY` in `~/.hermes/.env`. Default model deepseek-v4-flash.

## Subscription pricing (verified from repo docs 2026-08-11)

- **$5 for the first month, then $10/month** (flat). User pays $10/mo — NOT free; treat "zero cost" as wrong.
- Canonical source: `packages/web/src/content/docs/go.mdx` in the anomalycha/opencode repo (raw.githubusercontent fetch works; the rendered `opencode.ai/docs/go` page is a JS SPA that curl can't parse — fetch the .mdx raw instead). Full tree listing via `api.github.com/repos/anomalyco/opencode/git/trees/dev?recursive=1` to find doc paths.
- **Free-model fallback**: "If you reach the usage limit, you can continue using the free models" — a subset of models keeps working at $0 after the quota buckets are exhausted rather than blocking. This is the likely source of "$0 for go" claims — it's the fallback tier, not the subscription price.
- "With Go, you pay $10/month and we aim to give you 6x that in usage" — the $60/mo bucket ≈ 6× the subscription.

## Per-model rates & included monthly usage (from go.mdx table, 2026-08-11)

| Model | Input $/1M | Output $/1M | Cache-read $/1M | Cached-write $/1M | Included $/mo |
|---|---|---|---|---|---|
| Grok 4.5 | $2.00 | $6.00 | $0.30 | - | $15 |
| GPT 5.6 Luna (≤272K) | $0.20 | $1.20 | $0.02 | $0.25 | $15 |
| GPT 5.6 Luna (>272K) | $0.40 | $1.80 | $0.04 | $0.50 | $15 |
| GLM-5.2 / GLM-5.1 | $1.40 | $4.40 | $0.26 | - | $60 |
| Kimi K3 | $3.00 | $15.00 | $0.30 | - | $15 |
| Kimi K2.7 Code / K2.6 | $0.95 | $4.00 | $0.19 | - | $60 |
| MiMo V2.5 | $0.14 | $0.28 | $0.0028 | - | $60 |
| MiMo V2.5 Pro | $0.435 | $0.87 | $0.003625 | - | $15 |
| MiniMax M3/M2.7/M2.5 | $0.30 | $1.20 | $0.06 | - | $60 |
| Qwen3.8 Max | $2.00 | $6.00 | $0.25 | $2.50 | $15 |
| Qwen3.7 Max | $2.50 | $7.50 | $0.50 | $3.125 | $60 |
| Qwen3.7 Plus (≤256K) | $0.40 | $1.60 | $0.04 | $0.50 | $60 |
| Qwen3.7 Plus (>256K) | $1.20 | $4.80 | $0.12 | $1.50 | $60 |
| Qwen3.6 Plus (≤256K) | $0.50 | $3.00 | $0.05 | $0.625 | $60 |
| Qwen3.6 Plus (>256K) | $2.00 | $6.00 | $0.20 | $2.50 | $60 |
| DeepSeek V4 Pro | $0.435 | $0.87 | $0.003625 | - | $15 |
| DeepSeek V4 Flash | $0.14 | $0.28 | $0.0028 | - | $60 |
| Hy3 | $0.14 | $0.58 | $0.035 | - | $60 |

Key: "included $/mo" (the doc's **Usage** col) = dollar value of usage your $10/mo covers before the monthly cap. Most models get the full **6x multiplier** ($10 → $60); the $15 models (GPT 5.6 Luna, Qwen3.8 Max, Qwen3.7 Max, Kimi K3, MiMo V2.5 Pro, DeepSeek V4 Pro) only get **1.5x** ($10 → $15) — 4x less usage. **Do NOT read the "Cached Write" col as a usage multiplier** (it's the $/1M cache-write price; "-" = not charged). The "usage multiplier" is the $15-vs-$60 gap, not a per-token column. Effective request counts (per $12/5h bucket, from doc): DeepSeek V4 Pro 3,450 vs V4 Flash 31,650 — Flash yields ~9x more requests (≈ 4x included-usage × 3.1x token-price). Rate table in the doc may lag console reality — the doc says limits/pricing "may change as we learn from early usage".

## Limits (dollar-based buckets, enforced server-side)

- $12 / 5 hours
- $30 / 1 week
- $60 / 1 month

Enforced via `accountRateLimit` (visible in `strings` of `~/.opencode/bin/opencode`, "Go limit reached" dialog). No public quota API — dashboard-only. `opencode stats` is local-only session stats (SQLite), NOT quota.

## Ground truth: Hermes state.db

`~/.hermes/state.db` (SQLite) records every API call in `session_model_usage` (cols incl. `last_seen`, `api_call_count`, token counts). Query read-only: `python3 -c "import sqlite3; ...uri=file:...?mode=ro"` or `sqlite3 file:...?mode=ro`.

- `estimated_cost` = $0.0000 for Go (cost table lacks Go pricing) → token→$ math is manual.
- Filters: traffic to `opencode.ai/zen/go/*`; window = MIN/MAX `last_seen`.
- Note: calls landing in state.db later (rescue sessions) shift numbers slightly; re-run to refresh.

## Token→$ rates (per 1M tokens, from opencode.ai/docs/go pricing table)

| Model | Input | Output | Cache-read |
|---|---|---|---|
| deepseek-v4-flash | $0.14 | $0.28 | $0.0028 |
| pro / glm tiers | higher; see report script comment block | | |

Cached tokens ≈ 50× cheaper than fresh input — why cache-read-heavy usage stays cheap.

## Report scripts (both patched 2026-08-11 — keep in sync if rates change)

- `~/.hermes/scripts/token_usage_report.py` — 07:30 cron (no_agent, verbatim). Has `go_quota(since)` + `-- OpenCode Go quota --` section: 5h/7d/30d usage vs $12/$30/$60 caps, plus Go rates comment block.
- `~/.hermes/scripts/morning_context.py` — 07:00 brief context feed. One Go-quota summary line after totals.

## Pitfalls learned

- **The adaptive monitor catches unreachability, NOT throttling.** Its ping distinguishes 200 vs 000/502/5xx; a `-free` model can be reachable-but-429 (rate-limited). Before relying on the wired fallback, do an independent per-model curl with `x-opencode-session` (mandatory since the MissingSessionID enforcement) and, on a 429 first signal, switch fallback immediately — never retry the throttled model (09-07 #3).
- `errors.log` limit hits (526 found) traced to the OLD LiteLLM path `localhost:4000` → Google Vertex free tier, NOT Go. Zero Go limit errors since direct opencode-go routing. Don't attribute old-log hits to the current provider.
- CLI binary at `~/.opencode/bin/opencode` (not on PATH); credential store `~/.local/share/opencode/auth.json` — never print key values.
- `api.opencode.ai` is the real API host; `opencode.ai/api/*` → 404. No usage/quota endpoint exists.

## 2026-08-15 addendum (post $30 top-up incident — read this)

- Root causes of the freeze: (1) `fallback_model` was commented out in config.yaml — NO auto-failover existed (nothing to switch to on 429); (2) token_usage_report.py `GO_RATES` covered only 4 of ~21 models → anything else silently priced at flash rate; (3) script `DEFAULT_MODEL` was stale (`deepseek-v4-pro`) vs config (`deepseek-v4-flash`).
- FIXED in token_usage_report.py: full rate table incl. `glm-5.3` (GLM tier — ASSUMED, not in go.mdx), unknown-model self-warning line, bucket alarms at 50% (⚠️) / 80% (🔴), DEFAULT_MODEL=deepseek-v4-flash.
- CORRECT config key is **`fallback_providers.*`** (`provider`/`model`/`base_url`/`key_env`) — the `fallback_model:` block in config.yaml template is OUTDATED. Use `hermes config set fallback_providers.X Y` and `hermes config unset fallback_providers.X`.
- Current fallback (set 2026-08-17, verified via `hermes config get`): **opencode-go / hy3-free / `https://opencode.ai/zen/v1` / OPENCODE_GO_API_KEY** — a FREE-tier model on the same key, so a $30/wk dollar-bucket exhaustion now auto-switches to the free tier instead of freezing. (Previously nemotron-3-ultra-free, swapped 2026-08-17 when the adaptive monitor found it unhealthy; before that glm-5.2 on zen/go/v1 — same paid bucket, useless for bucket exhaustion.)
- METER vs SERVER lesson: state.db math said ~13% of the 7d bucket while the server enforced the cap. No quota API exists — when Go shows "limit reached", TRUST THE SERVER DIALOG over local math and switch model/provider immediately.
- Only LLM credential on this machine: OPENCODE_GO_API_KEY. `HERMES_CUSTOM_LOCALHOST_11434/4000_API_KEY` env entries are vestigial — nothing listens on those ports.
- REAL usage data: the Go dashboard lives at `https://app.opencode.ai/usage` (SPA; no API endpoint — /v1/usage etc. all "Not Found"; web-dashboard data is client-side, not scrapable). Verified 2026-08-15: real monthly spend ≈ $3.3 (Aug 11 $0.41, 12 $0.40, 13 $0.00 = freeze day, 14 $2.40). The original freeze was almost certainly the FREE-TIER weekly limit, not the paid $30/wk bucket — the meter was modeling the wrong plan tier. After upgrading to paid, state.db math (fixed table) now matches the dashboard within ~20%.
- **FREE MODELS WORK — corrected 2026-08-15** (earlier "not reachable" conclusion was WRONG — I tested only the paid endpoint zen/go/v1, where free models are intentionally "not supported", and generalized from one down upstream). Free models respond on **`https://opencode.ai/zen/v1/chat/completions`** with the SAME `OPENCODE_GO_API_KEY`, **unprefixed** IDs. Verified 200 OK: `hy3-free`, `nemotron-3-ultra-free`, `nemotron-3.5-lightning-free`. Currently down (503 "Endpoint is unavailable"): `deepseek-v4-flash-free`, `mimo-v2.5-free`, `laguna-s-2.1-free`. Free-tier usage is tracked separately and does NOT consume the paid $12/$30/$60 buckets — THE escape hatch for dollar-bucket exhaustion. `zen/v1/models` lists the full opencode catalog (claude-*, gemini-*) on this key.
- Burst guard added 2026-08-15: cron `go_bucket_watchdog.py` every 2h (08:00-22:00, no_agent, silent) — alerts only when 5h bucket ≥ 50% or 7d ≥ 60%. The 07:30 report can't see mid-day bursts; this closes that gap.
- **2026-08-18 addendum**: adaptive monitor detected `nemotron-3-ultra-free` healthy again (preference-order leader recovered) → fallback switched back from `hy3-free` to `nemotron-3-ultra-free` via `hermes config set fallback_providers.model nemotron-3-ultra-free` (verified: provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Catalogs unchanged (paid26/zen61/free6 identical), no GO_RATES pricing needed.
- **2026-08-18 addendum (later same day)**: adaptive monitor flipped fallback BACK to `hy3-free` (nemotron-3-ultra-free still present in free=6 but no longer top-healthy in the ping ranking). Re-set via `hermes config set fallback_providers.model hy3-free`, verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). This matches the pre-existing config state documented above — net config unchanged from the 08-17 baseline. Catalogs unchanged, no GO_RATES work.
- **2026-08-18 addendum (evening)**: adaptive monitor flipped fallback back to `nemotron-3-ultra-free` (top of preference order recovered) via `hermes config set fallback_providers.model nemotron-3-ultra-free`, verified via `hermes config get fallback_providers.model` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Catalogs otherwise unchanged (paid26/zen61/free6). ONLY price gap: `hy3-preview` appeared in paid26 with no GO_RATES entry → added to BOTH `token_usage_report.py` and `morning_context.py` as `# GUESS ≈ hy3` (0.14, 0.58, 0.035, 0.0); both scripts exit 0 with no unknown-model warnings. Free-tier leader swapped hy3-free↔nemotron-3-ultra-free twice today; both are healthy.
- **2026-08-18 addendum (evening) #2**: adaptive monitor saw `paid27` add one new model `muse-spark-1.2` (fallback/free/zen unchanged). No official rate available (anomalycha/opencode go.mdx returns 404 — repo moved/removed). Priced by mid-tier sibling analogy ≈ `qwen3.6-plus` (0.50, 3.00, 0.05, 0.625) marked `# GUESS` in BOTH `token_usage_report.py` and `morning_context.py`; both scripts exit 0 with no unknown-model warnings. Catalog now paid27/zen61/free6.
- **2026-08-19 addendum (adaptive monitor)**: fallback flipped back to **`nemotron-3-ultra-free`** (preference-order leader recovered/highest in ping ranking) via `hermes config set fallback_providers.model nemotron-3-ultra-free`, verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Catalogs unchanged (paid28/zen61/free6), no GO_RATES work. nemotron-3-ultra-free ↔ hy3-free continue to trade the leader spot daily; both remain healthy.
- **2026-08-19 addendum (later)**: adaptive monitor flipped fallback back to `hy3-free` (nemotron-3-ultra-free still in free=6 but dropped out of top-healthy ping ranking) via `hermes config set fallback_providers.model hy3-free`, verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Catalogs unchanged (paid28/zen61/free6), no GO_RATES work; both scripts exit 0 no unknown-model warnings. nemotron-3-ultra-free ↔ hy3-free continue trading the leader spot; both healthy.
- **2026-08-19 addendum (evening)**: adaptive monitor flipped fallback back to **`nemotron-3-ultra-free`** via `hermes config set fallback_providers.model nemotron-3-ultra-free`, verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Catalogs: paid28 unchanged; free= gained **`muse-spark-1.2-contributor-free`** (free6→free7, zen61→zen62). NEW free model evaluated: ping returns **HTTP 403 RegionError ("model not available in your country")** — region-blocked, NOT usable from here, and not in the preference order → does not affect fallback choice. No GO_RATES work needed (free models unpriced; both scripts exit 0, no unknown-model warnings). nemotron-3-ultra-free ↔ hy3-free continue trading the leader spot.
- **ADAPTIVE MONITOR added 2026-08-15**: cron `81a8bc241220` "Go adaptive monitor" (2-hourly, `monitor_script=adaptive_monitor.py`, agent fires ONLY when output hash changes). It pings the free-tier fallbacks in preference order (nemotron-3-ultra-free > hy3-free > nemotron-3.5-lightning-free > deepseek-v4-flash-free > mimo-v2.5-free > laguna-s-2.1-free) and fingerprints BOTH catalogs (`zen/v1/models` + `zen/go/v1/models`). On change the agent self-heals: re-wires `fallback_providers.model`, prices new paid models into both GO_RATES tables (sibling analogy + `# GUESS`), evaluates new `-free` models, updates this reference. Stable lines: `fallback=`, `free=`, `paidN=`, `zenN=`; `fetch=FAIL` is a STABLE failure string (no false fires). Test hooks: `ADAPT_ZEN_URL`/`ADAPT_GO_URL`/`ADAPT_CHAT_URL` env overrides. CRITICAL pitfall: python urllib gets HTTP 403 from opencode.ai's edge unless it sends a browser User-Agent (curl is fine) — the script hardcodes one.
- **2026-08-20 addendum (adaptive monitor)**: fallback flipped back to **`hy3-free`** (nemotron-3-ultra-free still returns upstream 502 "Nvidia: Service temporarily overloaded" on ping → fell out of top-healthy ranking) via `hermes config set fallback_providers.model hy3-free`, verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Catalogs: paid27 (muse-spark-1.2 REMOVED from paid list — its orphaned GO_RATES GUESS entry is harmless and left in place; no new paid models → no pricing work); free= +`x-preview-f-free` (free7→free8, zen62→zen63) — NEW free model evaluated: ping returns **HTTP 200 healthy**, but it is NOT in the preference order → kept hy3-free as fallback (conservative). Both scripts exit 0, no unknown-model warnings. nemotron-3-ultra-free ↔ hy3-free continue trading the leader spot; hy3-free currently healthy.
- **2026-08-20 addendum (later)**: adaptive monitor flipped fallback back to **`nemotron-3-ultra-free`** (preference-order leader recovered / top of ping ranking) via `hermes config set fallback_providers.model nemotron-3-ultra-free`, verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Catalogs unchanged (paid27/free8/zen63); no GO_RATES pricing needed. Both scripts exit 0, no unknown-model warnings. nemotron-3-ultra-free ↔ hy3-free continue trading the leader spot daily — this is the expected equilibrium, not a fault.
- **2026-08-21 addendum (adaptive monitor)**: paid27→paid29 added **`deepseek-v4-flash-vision-exp`** + **`ox-alpha-free`** (fallback/free8/zen63 unchanged; fallback still `nemotron-3-ultra-free`, healthy). Priced into BOTH `token_usage_report.py` + `morning_context.py`: `deepseek-v4-flash-vision-exp ≈ deepseek-v4-flash` (0.14, 0.28, 0.0028, 0.0) and `ox-alpha-free = $0` (0.0×4) — promo "limited time" model per live `opencode.ai/docs/go` (NOTE: rendered docs page IS parsable via Hermes web_extract, contradicting the earlier "JS SPA, curl can't parse" note — raw go.mdx fetch still 404s). Both marked `# GUESS`; both scripts exit 0, no unknown-model warnings. `ox-alpha-free` is paid-catalog-only (absent from zen63) — free-labeled but reachable only via zen/go/v1.
- **2026-08-21 addendum #2 (adaptive monitor)**: fallback flipped back to **`hy3-free`** via `hermes config set fallback_providers.0.model hy3-free` — NOTE: the plain `fallback_providers.model` form now CRASHES (`ValueError: invalid literal for int() with base 10: 'model'`) because `fallback_providers` is a LIST in config.yaml; the working form is the index-qualified `fallback_providers.0.model` (earlier addendums' command string is outdated). Verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Reason for flip: nemotron-3-ultra-free ping returned **HTTP 500** (upstream overloaded) → dropped out of top-healthy ranking; hy3-free ping returned **HTTP 200**. Catalogs unchanged (free8/paid29/zen63), no GO_RATES work; both scripts exit 0. Usual leader-trade equilibrium; hy3-free currently healthy.
- **2026-08-21 addendum #3 (adaptive monitor)**: fallback flipped back to **`nemotron-3-ultra-free`** via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (verified via `hermes config get fallback_providers`: provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Reason for flip: nemotron-3-ultra-free ping returned **HTTP 200** (recovered from the earlier 500); independent re-ping here confirmed 200. Catalogs unchanged (free8/paid29/zen63), no GO_RATES work (both scripts exit 0 as of addendum #2). Usual leader-trade equilibrium: nemotron-3-ultra-free ↔ hy3-free keeps swapping the top slot.
- **2026-08-21 addendum #4 (adaptive monitor)**: fallback flipped back to **`hy3-free`** via `hermes config set fallback_providers.0.model hy3-free` (index-qualified form — plain `fallback_providers.model` still crashes; see addendum #2). Verified via `hermes config get fallback_providers`: provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY. Reason: nemotron-3-ultra-free dropped out of top-healthy ping ranking again (usual upstream flakiness); hy3-free is the monitor's top-healthy pick. Catalogs unchanged (free8/paid29/zen63), no GO_RATES work. Same daily leader-trade equilibrium — third flip this day; both models healthy, no action beyond re-wiring the fallback.
- **2026-08-21 addendum #5 (adaptive monitor)**: fallback flipped back to **`nemotron-3-ultra-free`** via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Independent re-ping here returned **HTTP 200** healthy (cost "0", 5/5 tokens). Catalogs unchanged (free8/paid29/zen63), no GO_RATES work (both scripts exit 0, no unknown-model warnings). Fourth flip this day — the usual nemotron-3-ultra-free ↔ hy3-free leader trade continues; both healthy.
- **2026-08-22 addendum (adaptive monitor)**: fallback flipped back to **`hy3-free`** via `hermes config set fallback_providers.0.model hy3-free` (index-qualified form; plain `fallback_providers.model` still crashes — see 2026-08-21 #2). Verified via `hermes config get fallback_providers`: provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY. Independent re-ping here returned **HTTP 200** healthy (5/5 tokens, finish_reason length). Reason: nemotron-3-ultra-free dropped out of top-healthy ping ranking again. Catalogs unchanged (free8/paid29/zen63), no GO_RATES work (both scripts exit 0, no unknown-model warnings). Leadership trade equilibrium continues into its fifth day.
- **2026-08-22 addendum #2 (adaptive monitor)**: fallback flipped back to **`nemotron-3-ultra-free`** via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Independent re-ping here returned **HTTP 200** healthy (5/5 tokens, finish_reason length; usage 17 prompt / 5 completion). Reason: nemotron-3-ultra-free (preference-order leader) recovered to top-healthy in the monitor's ping ranking, overtaking hy3-free. Catalogs unchanged (free8/paid29/zen63), no GO_RATES work needed. The nemotron-3-ultra-free ↔ hy3-free leader trade continues — second flip today, fifth day of the equilibrium.
- **2026-08-26 addendum (adaptive monitor)**: paid29→paid31 added **`glm-5.3-flash`** + **`grok-4.6`**; removed **`ox-alpha-free`** (paid) and **`x-preview-f-free`** (free8→free7, zen63→zen62). Fallback UNCHANGED — **`nemotron-3-ultra-free`** still preference-order leader and present in free list; verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). No official rates for the new models (go.mdx still 404 — repo gone). Priced into BOTH `token_usage_report.py` + `morning_context.py`: `grok-4.6 ≈ grok-4.5` (2.00, 6.00, 0.30, 0.0) and `glm-5.3-flash ≈ glm-5.3` (1.40, 4.40, 0.26, 0.0; note "flash tier may price lower") — both marked `# GUESS`. Both scripts exit 0, no unknown-model warnings. Removals: `ox-alpha-free` GUESS entry orphaned but harmless, left in place (per 08-20 muse-spark-1.2 precedent); `x-preview-f-free` was a `-free` model with no GO_RATES entry — nothing to clean. No fallback flip this cycle; the nemotron ↔ hy3 leader trade paused on nemotron-3-ultra-free (healthy).
- **2026-08-28 addendum (adaptive monitor)**: fallback flipped back to **`hy3-free`** via `hermes config set fallback_providers.0.model hy3-free` (index-qualified form), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY). Reason: nemotron-3-ultra-free dropped out of top-healthy ping ranking (usual upstream flakiness). paid31→paid33 added **`hy4-preview`** + **`qwen3.8-flash`**; free7→free8 (zen62→zen63) added **`ling-3.0-flash-fin-free`**. No official rates (go.mdx still 404) → priced into BOTH `token_usage_report.py` + `morning_context.py`: `hy4-preview ≈ hy3` (0.14, 0.58, 0.035, 0.0) and `qwen3.8-flash ≈ qwen3.8-max` (2.00, 6.00, 0.25, 2.50) — both `# GUESS`. NEW free model `ling-3.0-flash-fin-free` pinged: **HTTP 200 healthy** (5/5 tokens, finish_reason length) but NOT in the preference order → kept hy3-free as fallback (conservative, x-preview-f-free precedent). Both scripts exit 0, no unknown-model warnings. Usual leader-trade equilibrium — nemotron ↔ hy3 continues.
- **2026-08-28 addendum #2 (adaptive monitor, same day)**: monitor flipped fallback to **`nemotron-3-ultra-free`** (preference-order leader appeared top-healthy in its tick). INDEPENDENT re-ping here contradicted: nemotron-3-ultra-free returned edge HTTP 200 but body **502 "Nvidia: Service temporarily overloaded"** (the documented unhealthy signature) while **hy3-free completed end-to-end** (real 5/5-token completion, cost "0", finish_reason length). To never leave the fallback wired to a model that is erroring right now, reset via `hermes config set fallback_providers.0.model hy3-free`, verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY) — net config identical to pre-tick state. Catalogs unchanged from addendum #1 (free8/paid33/zen63), no GO_RATES work (monitor diff showed only the fallback= line changed; both scripts exit 0). Lesson reinforced: the monitor's tick is a snapshot — a one-shot edge-200 can mask an upstream 502; when the fallback's health matters, verify completion, not just HTTP status.
- **2026-08-28 addendum #3 (adaptive monitor, same day)**: fallback flipped back to **`hy3-free`** via `hermes config set fallback_providers.0.model hy3-free` (index-qualified form; plain `fallback_providers.model` re-confirmed crashing with `ValueError: invalid literal for int()` — the list-index form is mandatory, see 2026-08-21 #2). Verified via `hermes config get fallback_providers`: provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY (second entry, local gemma fallback, untouched). INDEPENDENT re-ping of hy3-free: **HTTP 200, real 5/5-token completion** (finish_reason length, reasoning_tokens 5, cost "0") — genuinely healthy, no masked 502. Reason: nemotron-3-ultra-free dropped out of top-healthy ping ranking (usual upstream flakiness). Monitor diff showed ONLY the fallback= line changed (free8/paid33/zen63 identical) — no GO_RATES work, no new-model evaluation. Third fallback flip today (hy3 → nemotron → hy3); the nemotron ↔ hy3 leader equilibrium continues.
- **2026-08-28 addendum #4 (adaptive monitor, same day)**: monitor flipped fallback to **`nemotron-3-ultra-free`** (preference-order leader top-healthy in its tick). INDEPENDENT re-ping here CONFIRMED genuine health this time: nemotron-3-ultra-free returned **HTTP 200 with a real 5/5-token completion** (finish_reason length, 17 prompt / 5 completion tokens — NO masked 502, unlike addendum #2's edge-200 trap). hy3-free also healthy (200, 5/5, cost "0"). Set via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). Monitor diff showed ONLY the fallback= line changed (free8/paid33/zen63 identical) → no GO_RATES work; both report scripts exit 0, no unknown-model warnings. Fourth flip today (hy3 → nemotron → hy3 → nemotron); the nemotron ↔ hy3 leader equilibrium continues. Config now matches the preference-order leader.
- **2026-08-28 addendum #5 (adaptive monitor, same day)**: fallback flipped back to **`hy3-free`** via `hermes config set fallback_providers.0.model hy3-free` (index-qualified form; plain `fallback_providers.model` still crashes), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping confirmed genuine health: **HTTP 200, real 5/5-token completion** (finish_reason length, 13 prompt / 5 completion tokens, reasoning_tokens 5, cost "0") — no masked 502. Reason: nemotron-3-ultra-free dropped out of top-healthy ping ranking (usual upstream flakiness). Monitor diff showed ONLY the fallback= line changed (free8/paid33/zen63 identical) → no GO_RATES work; both report scripts exit 0, no unknown-model warnings. Fifth flip today (hy3 → nemotron → hy3 → nemotron → hy3); the nemotron ↔ hy3 leader equilibrium continues.
- **2026-08-28 addendum #6 (adaptive monitor)**: fallback flipped back to **`nemotron-3-ultra-free`** via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping CONFIRMED genuine health: **HTTP 200, real 5/5-token completion** (finish_reason length, 21 prompt / 5 completion tokens, cost "0") — no masked 502; hy3-free also 200/5-token healthy (17/5). Reason: nemotron-3-ultra-free (preference-order leader) top of the monitor's ping ranking, corroborated here. Monitor diff showed ONLY the fallback= line changed (free8/paid33/zen63 identical) → no GO_RATES work; both report scripts exit 0, no unknown-model warnings. ALSO refreshed `~/.hermes/backups/known-good/config.yaml` by hand (was stale from 08-21 — NOTE: llm-watchdog.sh only RESTORES known-good, it never refreshes it; the "refresh on config changes" memory note is inaccurate, manual sync is the practice). Sixth flip today (hy3 → nemotron → hy3 → nemotron → hy3 → nemotron); the nemotron ↔ hy3 leader equilibrium continues.
- **2026-08-29 addendum (adaptive monitor)**: fallback flipped back to **`hy3-free`** via `hermes config set fallback_providers.0.model hy3-free` (index-qualified form; plain form still crashes, see 2026-08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping confirmed genuine health: **hy3-free HTTP 200, real completion** (finish_reason length, cost "0") — no masked 502; nemotron-3-ultra-free ALSO 200 with a real completion (5/5, finish_reason length) — both genuinely healthy this tick, but the monitor's ranking put hy3-free top. Monitor diff showed ONLY the fallback= line changed (free8/paid33/zen63 identical) → no GO_RATES work, no new-model evaluation; both report scripts exit 0. Known-good config.yaml re-synced by hand (08-28 #6 practice). The nemotron ↔ hy3 leader equilibrium continues; hy3-free currently wired.
- **2026-08-29 addendum #2 (adaptive monitor)**: fallback flipped back to **`nemotron-3-ultra-free`** (preference-order leader top-healthy in monitor tick) via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping CONFIRMED genuine health: **HTTP 200, real 5/5-token completion** (finish_reason length, actual content in body — NO masked 502, unlike the 08-28 #2 edge-200 trap). Monitor diff showed ONLY the fallback= line changed (free8/paid33/zen63 identical) → no GO_RATES work, no new-model evaluation; both report scripts re-run this tick and exit 0. Known-good config.yaml re-synced by hand (08-28 #6 practice). Seventh flip in two days (hy3 → nemotron); the nemotron ↔ hy3 leader equilibrium continues, nemotron-3-ultra-free currently wired.
- **2026-08-29 addendum #3 (adaptive monitor)**: fallback flipped back to **`hy3-free`** via `hermes config set fallback_providers.0.model hy3-free` (index-qualified form; plain form still crashes), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping: hy3-free **HTTP 200 real completion** (finish_reason length, content present — no masked 502); nemotron-3-ultra-free **HTTP 000** (20s connect timeout — genuinely unreachable this tick, corroborates monitor's demotion). Diff showed ONLY fallback= changed (free8/paid33/zen63 identical) → no GO_RATES work; sampled 10 recently-added paid models in both scripts' GO_RATES (16/11 hits) — coverage intact. NOTE: cron approval guard now BLOCKS shell `cp`/`rm`/`python -c` for config/backup ops (approvals.cron_mode not set) — known-good re-sync done via the patch tool instead (verified byte-identical delta: file sizes differ only by the 13-char model string swap). Eighth flip in two days; hy3-free currently wired.
- **2026-08-29 addendum #4 (adaptive monitor)**: monitor tick flipped fallback to **`nemotron-3-ultra-free`** (preference-order leader top-healthy in its ping). INDEPENDENT re-ping here CONTRADICTED: nemotron-3-ultra-free returned the **edge HTTP 200 masked-502 trap** (`{"error":{"type":"server_error" ... [502] Upstream error from Nvidia: Service temporarily overloaded"}}` — exactly the 08-28 #2 signature), while **hy3-free completed end-to-end** (HTTP 200, real 5/5-token completion, finish_reason length, cost "0"). Per the 08-28 #2 lesson (never wire the fallback to a model erroring right now), made NO change — config stays `hy3-free`, net state identical to pre-tick. Diff showed ONLY the fallback= line changed (free8/paid33/zen63 identical) → no GO_RATES work, no new-model evaluation. Ninth flip tick in two days; the nemotron ↔ hy3 leader equilibrium continues, monitor tick DOWNGraded (snapshot vs completion-verified health).
- **2026-08-29 addendum #5 (adaptive monitor)**: tick transitioned from `fetch=FAIL` (previous run's stable failure string) to a full healthy snapshot: `fallback=nemotron-3-ultra-free`, free8/paid33/zen63 — catalogs identical to the 08-28/08-29 baseline, so no new models, no pricing work. INDEPENDENT re-ping CONFIRMED genuine health: **nemotron-3-ultra-free HTTP 200, real 5/5-token completion** (content present "The user wants a greeting", finish_reason length, 22 prompt/5 completion, cost "0" — NO masked 502); hy3-free also healthy (200, 5/5 reasoning tokens, cost "0"). Monitor pick corroborated → set via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). Known-good config.yaml re-synced by hand via patch tool (08-28 #6 practice; shell cp still approval-guarded). Both report scripts exit 0, no unknown-model warnings. NOTE (no action): paid33 contains 5 models with NO GO_RATES entry in either script — `kimi-k2.5`, `longcat-2.0`, `mimo-v2-omni`, `mimo-v2-pro`, `qwen3.5-plus` (pre-existing, not new this tick; unpriced-but-unused — no state.db traffic, so no warnings). Sibling analogies if ever priced: kimi-k2.5 ≈ kimi-k2.6; mimo-v2-omni ≈ mimo-v2.5; mimo-v2-pro ≈ mimo-v2.5-pro; qwen3.5-plus ≈ qwen3.6-plus; longcat-2.0 unknown tier (leave to flash default).
- **2026-08-29 addendum #6 (adaptive monitor)**: `hy3-free` REMOVED from zen/v1 catalog (free8→free7, zen63→zen62 — sole diff this tick; paid33 identical). Independent ping confirmed real removal, not a listing hiccup: **HTTP 401 `Model hy3-free is not supported`** on zen/v1 chat completions (vs the 08-15/08-28/08-29 records of 200 OK). The nemotron ↔ hy3 leader-trade equilibrium is therefore OVER — hy3-free is gone from the free tier. Fallback UNCHANGED: `nemotron-3-ultra-free` (preference-order leader) still present in free= and independently verified **HTTP 200 real completion** — no config change, no GO_RATES work (free models unpriced; both scripts exit 0). Preference order effectively shortens to nemotron-3-ultra-free > nemotron-3.5-lightning-free > deepseek-v4-flash-free > mimo-v2.5-free > laguna-s-2.1-free. Paid `hy3` / `hy3-preview` still in paid33 (paid tier unaffected); if the leader-trade pattern was expected to continue, monitor it — next competitor for the top spot should come from the remaining preference list.
- **2026-08-30 addendum (adaptive monitor)**: tick went `fetch=FAIL` (previous run) → full healthy snapshot: `fallback=nemotron-3.5-lightning-free`, free7/paid33/zen62 (catalogs unchanged from the 08-29 post-hy3-removal baseline). FIRST-TIME fallback= value (nemotron-3.5-lightning-free): nemotron-3-ultra-free (the preference-order leader) is now unhealthy — independent ping showed the known **edge-200 masked-502 signature** (`server_error ... [502] Upstream error from Nvidia: Service temporarily overloaded` — same as 08-28 #2 / 08-29 #4); meanwhile nemotron-3.5-lightning-free completed end-to-end (HTTP 200, real 5/5 completion). Monitor pick corroborated → set via `hermes config set fallback_providers.0.model nemotron-3.5-lightning-free` (index-qualified form; plain form still crashes), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). NEW/CHANGED free models evaluated: `ling-3.0-flash-fin-free` now **HTTP 200 real completion** (healthy — and now the TOP-healthy free model after nemotron-ultra and hy3-free, but NOT in preference order → conservative, no switch; x-preview-f-free precedent); `muse-spark-1.2-contributor-free` returned **HTTP 500** (was 403 region-blocked on 08-19; still unusable either way — region-blocked/down, irrelevant to fallback choice). PAID PRICING WORK: the 5 models flagged unpriced in #5 (`kimi-k2.5`, `longcat-2.0`, `mimo-v2-omni`, `mimo-v2-pro`, `qwen3.5-plus`) added to BOTH `token_usage_report.py` + `morning_context.py` as `# GUESS` by sibling analogy: `kimi-k2.5 ≈ kimi-k2.6` (0.95, 4.00, 0.19, 0.0); `qwen3.5-plus ≈ qwen3.6-plus` (0.50, 3.00, 0.05, 0.625); `mimo-v2-pro ≈ mimo-v2.5-pro` (0.435, 0.87, 0.003625, 0.0); `mimo-v2-omni ≈ mimo-v2.5-pro` (aimed high for omni/multimodal — deliberate overcount bias so quota awareness errs conservative); `longcat-2.0 = glm-5 tier` (1.40, 4.40, 0.26, 0.0) — deviates from #5's "leave to flash default" suggestion with a mid-tier explicit rate (any explicit rate beats silent flash-pricing; unknown-model warnings now impossible for the full paid33). Both scripts exit 0, no unknown-model warnings. Known-good config.yaml re-synced by hand (08-28 #6 practice; patch tool, shell cp still approval-guarded).
- **2026-08-30 addendum #2 (adaptive monitor)**: fallback flipped back to **`nemotron-3-ultra-free`** (preference-order leader recovered — previous tick had filled in with nemotron-3.5-lightning-free after ultra showed the masked-502 signature) via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping CONFIRMED genuine health: **HTTP 200, real 5/5-token completion** (finish_reason length, content present, 17 prompt/5 completion tokens, cost "0" — NO masked 502; ~18s latency, slow edge). Monitor diff showed ONLY the fallback= line changed (free7/paid33/zen62 identical — hy3-free still absent per 08-29 #6) → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool). Recovery tick — ultra re-wired as fallback after its first-time demotion.
- **2026-08-31 addendum (adaptive monitor)**: fallback flipped to **`nemotron-3.5-lightning-free`** (preference-order leader nemotron-3-ultra-free demoted again — the 08-30 #1 pattern) via `hermes config set fallback_providers.0.model nemotron-3.5-lightning-free` (index-qualified form), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping: BOTH genuinely healthy this tick — lightning **HTTP 200 real 5/5-token completion** (0.65s, cost "0"); ultra also HTTP 200 real 5/5 (7.3s latency — slow edge, consistent with the monitor demoting it; no masked 502 either way). Monitor diff showed ONLY the fallback= line changed (free7/paid33/zen62 identical — hy3-free still absent per 08-29 #6) → no GO_RATES work, no new-model evaluation; both report scripts exit 0, no unknown-model warnings. Known-good config.yaml re-synced by hand (patch tool). Leader-trade equilibrium continues — now ultra ↔ lightning, hy3-free gone.
- **2026-08-31 addendum #2 (adaptive monitor)**: fallback flipped back to **`nemotron-3-ultra-free`** (preference-order leader recovered — prior tick had wired nemotron-3.5-lightning-free after ultra showed slow-edge latency) via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping CONFIRMED genuine health: BOTH models healthy — ultra **HTTP 200 real 5/5-token completion** (finish_reason present, no error payload — NO masked 502); lightning also HTTP 200 real completion. Monitor diff showed ONLY the fallback= line changed (free7/paid33/zen62 identical — hy3-free still absent per 08-29 #6) → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool). Leader-trade equilibrium continues — ultra recovered the top slot from lightning after a one-tick demotion.
- **2026-08-31 addendum #3 (adaptive monitor)**: fallback flipped back to **`nemotron-3.5-lightning-free`** (preference-order leader nemotron-3-ultra-free demoted again — the ongoing ultra ↔ lightning trade) via `hermes config set fallback_providers.0.model nemotron-3.5-lightning-free` (index-qualified form; plain `fallback_providers.model` re-confirmed crashing with `ValueError` this tick — the list-index form is mandatory, see 2026-08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping CONFIRMED genuine health: lightning **HTTP 200, real 5/5-token completion** (finish_reason length, content present, 17 prompt/5 completion tokens, cost "0" — NO masked 502). Ultra not independently re-pinged this tick (tool-call budget); monitor's demotion is consistent with its slow-edge latency pattern from #1. Monitor diff showed ONLY the fallback= line changed (free7/paid33/zen62 identical — hy3-free still absent per 08-29 #6) → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool). Leader-trade equilibrium: ultra ↔ lightning continues; lightning now wired.
- **2026-09-01 addendum (adaptive monitor)**: FIRST-EVER `fallback=ALL-DEAD` tick — the monitor's preference-order checks deemed every free model unhealthy in its snapshot (priors: fallback=nemotron-3.5-lightning-free from 08-31 #3; catalogs free7/paid33/zen62 identical — ONLY the fallback= line changed, no pricing work). INDEPENDENT re-pings CONTRADICTED the monitor (completion-verified, not edge-200): **nemotron-3.5-lightning-free HTTP 200 real 5/5 completion (healthy — the wired model)**; nemotron-3-ultra-free masked-200 Nvidia [502] "Service temporarily overloaded" (dead — same 08-28 #2 signature); deepseek-v4-flash-free HTTP 400 "Model is unavailable" (dead); mimo-v2.5-free, laguna-s-2.1-free, ling-3.0-flash-fin-free all HTTP 200 real completions (healthy; ling not in preference order → no switch, x-preview-f-free precedent); muse-spark-1.2-contributor-free HTTP 500 (down). So 4/7 free models were genuinely healthy this tick — ALL-DEAD was a FALSE NEGATIVE (monitor snapshot caught Nvidia-hosted upstream flakiness; ultra+lightning are Nvidia-backed, the healthy trio isn't). CONFIG: plain `fallback_providers.model` crashed AGAIN (ValueError — list-index form mandatory, 08-21 #2); re-asserted via `hermes config set fallback_providers.0.model nemotron-3.5-lightning-free`, verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched) — no-op re-write, net config identical to pre-tick. Known-good config.yaml NOT re-synced (no effective change). Lesson: trust ALL-DEAD as little as any edge verdict — verify a completion end-to-end before believing it; ultra ↔ lightning leader trade continues, lightning wired.
- **2026-09-01 addendum #2 (adaptive monitor)**: RECOVERY tick — fallback line self-corrected `ALL-DEAD` → `nemotron-3.5-lightning-free` (tick #1's ALL-DEAD was a false negative; 4/7 free models were genuinely healthy, see #1). INDEPENDENT re-ping CONFIRMED genuine health: **HTTP 200, real 5/5-token completion** (finish_reason length, content present "Here's a thinking process", 17 prompt / 5 completion tokens, cost "0" — NO masked 502, nothing like the Nvidia [502] overload signature; ultra NOT re-pinged, budget). CONFIG: already wired to `nemotron-3.5-lightning-free` from tick #1's no-op re-assert — verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched) → NO config change this tick. CATALOG: zen62→zen63 added **`claude-fable-5-1`** (zen/v1 full-catalog listing only; NOT in paid33, NOT a `-free` model) → no GO_RATES work (paid33/free7 unchanged; both report scripts exit 0 per tick #1). Known-good config.yaml NOT re-synced (no effective change). The ultra ↔ lightning leader trade continues; lightning wired.
- **2026-09-02 addendum (adaptive monitor)**: HISTORIC first-time flip to **`mimo-v2.5-free`** — the leader trade chain broke: the two Nvidia-backed leaders (nemotron-3-ultra-free, nemotron-3.5-lightning-free) BOTH timed out (HTTP 000, no response within 30s) in the monitor snapshot AND in independent re-pings here; deepseek-v4-flash-free returned HTTP 400 "Model is unavailable" (server_error — still dead since the earlier 400 record). `mimo-v2.5-free` was the first preference-order model to answer: **HTTP 200, real completion** (finish_reason length, 248 prompt / 5 completion tokens, 192 cached, cost "0" — NO masked 502). Set via `hermes config set fallback_providers.0.model mimo-v2.5-free` (index-qualified form; plain form still crashes), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). Catalogs identical to 09-01 #2 (free7/paid33/zen63) — ONLY the fallback= line changed → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool). Meaning: with both nemotrons down, the effective preference order drops to deepseek-v4-flash-free (dead) > **mimo-v2.5-free (current)** > laguna-s-2.1-free. Ultra/lightning status should be re-checked next tick; if they recover, expect the flip back.
- **2026-09-02 addendum #2 (adaptive monitor)**: fallback flipped `mimo-v2.5-free` → **`nemotron-3.5-lightning-free`** (paid33→paid34, free7→free8, zen63→zen65). INDEPENDENT re-ping confirmed: nemotron-3-ultra-free still **masked edge-200 [502] Nvidia \"Service temporarily overloaded\"** (dead — consistent with this morning's timeouts); lightning **HTTP 200 real 5/5-token completion** (cost \"0\", 18/5 tokens) — monitor pick corroborated → set via `hermes config set fallback_providers.0.model nemotron-3.5-lightning-free` (index-qualified form; plain form still crashes), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). NEW PAID model `muse-spark-1.3-contributor` (paid33→34) priced into BOTH `token_usage_report.py` + `morning_context.py` as `# GUESS ≈ muse-spark-1.2-contributor` (0.50, 3.00, 0.05, 0.625); both scripts exit 0, no unknown-model warnings. NEW FREE model `muse-spark-1.3-contributor-free` (free7→8, zen63→65) pinged: **HTTP 500 internal server error** (down; same signature as its 1.2 sibling in go-ping.log) and NOT in preference order → no switch (conservative, x-preview-f-free precedent). `gemini-3.8-flash` added to zen/v1 catalog only (not in paid list) → no GO_RATES work. Known-good config.yaml re-synced by hand (patch tool). With ultra still overloaded, lightning is the effective preference-order leader; mimo-v2.5-free was the emergency fill, now superseded.
- **2026-09-04 addendum (adaptive monitor)**: paid34→paid35 added **`omen-alpha`** (sole diff this tick; fallback/free8/zen65 all unchanged — fallback still `nemotron-3.5-lightning-free`, verified via `hermes config get fallback_providers`, provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched; no config change). NEW PAID model priced with OFFICIAL rates — no GUESS needed: raw go.mdx still 404 (repo gone), but live **rendered `opencode.ai/docs/go` NOW parses via plain curl with a browser User-Agent** (server-rendered HTML, tables extractable — confirms/upgrades the 08-21 web_extract note) and lists **Omen Alpha: $0.20 input / $0.66 output / $0.04 cache-read / no cache-write charge, Usage $100/mo (highest on the sheet), 11,600 req/5h / 29,000 wk / 57,900 mo**. Added exact tuple `(0.20, 0.66, 0.04, 0.0)` to BOTH `token_usage_report.py` + `morning_context.py` (marked VERIFIED 2026-09-04, not GUESS); both scripts exit 0, no unknown-model warnings. NOTE (rate-table drift, NO action taken this tick — narrow diff policy): the live docs now show several rates differing from script entries — grok-4.6 cache-read $0.50 (+>200K tiers $4/$12/$1), kimi-k2.6 cache-read $0.16, longcat-2.0 $0.30/$1.20/$0.006 (usage $60), muse-spark-1.2/1.3-contributor $0.10/$0.20/$0.002, qwen3.8-flash $0.15/$0.47/$0.016/$0.20 (usage $30), glm-5.3-flash $0.15/$0.50/$0.03, hy4-preview $0.834/$2.501/$0.042 (usage $30), minimax-m2.5/m2.7 cache-write $0.375, qwen3.7-max usage $30, deepseek-v4-* now peak/off-peak tiered. A dedicated full re-sync pass (both scripts + this ref) is warranted when a non-urgent cycle can absorb it.
|- **2026-09-04 addendum #2 (adaptive monitor)**: zen/v1 catalog grew zen65→zen70 (sole diff this tick; paid35/free8 unchanged, fallback unchanged — `nemotron-3.5-lightning-free`, re-verified via `hermes config get fallback_providers`: provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched; no config change). 5 models added to zen listing: `deepseek-v4-flash-vision-exp`, `glm-5.3`, `glm-5.3-flash` (all 3 already in paid35 with GO_RATES entries — no pricing work), plus **`gpt-6-astra`** and **`muse-spark-1.3`** which are NEW zen-only listings (NOT in paid35, NOT `-free` → not billed on the zen/go meter, not fallback candidates). No GO_RATES work (both scripts exit 0, no unknown-model warnings; meter 5h 1.7% / 7d 8.9% / 30d 18.7%). NOTE for future ticks: if `gpt-6-astra` or `muse-spark-1.3` appear in paidN, price by sibling analogy (gpt-6-astra ≈ gpt-5.6-luna tier; muse-spark-1.3 ≈ muse-spark-1.2) — no official rates expected (go.mdx 404, rendered docs may have them).
|- **2026-09-04 addendum #3 (adaptive monitor)**: fallback flipped back to **`nemotron-3-ultra-free`** (preference-order leader recovered / top-healthy in monitor tick; prior wired model nemotron-3.5-lightning-free) via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping CONFIRMED genuine health: **HTTP 200, real 5/5-token completion** (finish_reason length, content present — NO masked Nvidia-502 signature); old model nemotron-3.5-lightning-free also 200/healthy (corroborates the ultra ↔ lightning leader-trade equilibrium continuing). CATALOG CHANGE: **`laguna-s-2.1-free` REMOVED from free tier** — free8→free7, zen70→zen69 (sole catalog diffs; paid35 unchanged). Independent ping confirmed real removal: **HTTP 401 `Model laguna-s-2.1-free is not supported`** (same signature as hy3-free on 08-29 #6). laguna was never the wired fallback (preference order #6) → removal is a no-op for config; note the effective preference order shortens to nemotron-3-ultra-free > nemotron-3.5-lightning-free > deepseek-v4-flash-free > mimo-v2.5-free. No GO_RATES work (paid35 identical, both scripts exit 0, no unknown-model warnings). Known-good config.yaml re-synced by hand (patch tool). Ultra ↔ lightning leader trade continues; ultra wired.
- **2026-09-05 addendum (adaptive monitor)**: fallback flipped back to **`nemotron-3.5-lightning-free`** (preference-order leader nemotron-3-ultra-free demoted; prior wired model was nemotron-3-ultra-free from 09-04 #3) via `hermes config set fallback_providers.0.model nemotron-3.5-lightning-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping: lightning **HTTP 200 real 5/5-token completion** (finish_reason length, content present — NO masked-502 signature, genuinely healthy); ultra **HTTP 000** (curl connect/timeout — no response, the hard-down signature seen on 09-02, harsher than the masked-502 edge trap). Monitor diff showed ONLY the fallback= line changed (free7/paid35/zen69 identical — laguna still absent per 09-04 #3) → no GO_RATES work, no new-model evaluation; both scripts exit 0, no unknown-model warnings. Ultra ↔ lightning leader trade continues; ultra hard-down this tick, lightning wired.
- **2026-09-06 addendum (adaptive monitor)**: fallback flipped `nemotron-3.5-lightning-free` → **`mimo-v2.5-free`** (second time mimo becomes the emergency fill — same signature as 09-02 #1; prior wired model was lightning from 09-05) via `hermes config set fallback_providers.0.model mimo-v2.5-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping: mimo-v2.5-free **HTTP 200 real completion** (finish_reason length, 249 prompt/5 completion, 192 cached, cost "0" — NO masked 502); nemotron-3.5-lightning-free ALSO HTTP 200 real completion (genuinely healthy in this independent ping, but demoted in the monitor's snapshot — Nvidia-backed latency/upstream flakiness pattern); nemotron-3-ultra-free **HTTP 000 timeout** (hard down, same as 09-02). Monitor pick corroborated → wired mimo. Catalogs identical (free7/paid35/zen69 — laguna still absent per 09-04 #3, hy3 still gone per 08-29 #6) → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool — its copy was stale at nemotron-3-ultra-free, predating even the 09-05 lightning flip). With ultra hard-down and lightning Nvidia-flaky, mimo is the effective preference-order leader; expect a flip back when a nemotron recovers.
- **2026-09-06 addendum #2 (adaptive monitor)**: fallback flipped back `mimo-v2.5-free` → **`nemotron-3.5-lightning-free`** (the 09-02 #2 pattern — emergency fill superseded when a nemotron recovered) via `hermes config set fallback_providers.0.model nemotron-3.5-lightning-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping confirmed genuine health: **HTTP 200, real 5/5-token completion** (finish_reason length, content present "Here's a thinking process", 17 prompt / 5 completion tokens, reasoning_tokens 7, cost "0" — NO masked 502). Monitor diff showed ONLY the fallback= line changed (free7/paid35/zen69 identical — laguna still absent per 09-04 #3, hy3 still gone per 08-29 #6) → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool — was stale at mimo-v2.5-free, the 09-06 #1 wiring). nemotron-3-ultra-free remains in free list but not top-healthy (usual Nvidia-backed flakiness; expected per the ultra ↔ lightning leader trade); lightning now wired — 40-min emergency fill ended.
- **2026-09-07 addendum (adaptive monitor)**: tick went `fetch=FAIL` (prev) → full snapshot with **`fallback=ALL-DEAD`** (second all-dead event; catalogs identical to the 09-06 #3 baseline — free7/paid35/zen69, no new models, no GO_RATES work). INDEPENDENT re-pings CONTRADICTED ALL-DEAD: the free tier is NOT dead — with an `x-opencode-session` header, `nemotron-3-ultra-free` returns **HTTP 200 real completion** (17 prompt/5 completion, finish_reason length — NO masked 502), `nemotron-3.5-lightning-free` HTTP 200, `mimo-v2.5-free` HTTP 200; dead only: deepseek-v4-flash-free 400 "Model is unavailable" (unchanged), ling-3.0-flash-fin-free 503 "Endpoint is unavailable", muse-spark-1.2/1.3-contributor-free 500. ROOT CAUSE of the false ALL-DEAD: pings without any `x-opencode-session` header now return **HTTP 400 `MissingSessionID` "OpenCode's free tier can only be used in OpenCode"** (new gateway enforcement) — the monitor script ALREADY sends `x-opencode-session: sess-adaptive-monitor` (since 09/06, line 46), and that session is still accepted (200) — the real killer was **latency: the Nvidia-backed free edge took 29–34s to respond**, far beyond `PING_TIMEOUT = 8`. FIXED: raised `PING_TIMEOUT` 8→30 in `~/.hermes/scripts/adaptive_monitor.py` (comment documents it; output strings unchanged on healthy ticks → hash trigger unaffected). After the fix the monitor self-tick reports `fallback=nemotron-3.5-lightning-free` (ultra failed even the 30s window this run — its slow-edge demotion signature, cf. 08-31 #1) → re-wired via `hermes config set fallback_providers.0.model nemotron-3.5-lightning-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). Known-good config.yaml re-synced by hand (patch tool). Lessons: (1) ALL-DEAD stays suspect until completion-verified with the CURRENT request shape — a MissingSessionID 400 hits every model and mimics all-dead; (2) the free tier now requires `x-opencode-session` on chat completions; any external pinger must send one; (3) 8s ping timeout is too tight for the current slow edge.
|- **2026-09-07 addendum #3 (adaptive monitor)**: fallback flipped `mimo-v2.5-free` → **`nemotron-3-ultra-free`** (preference-order leader recovered; prior wired model was mimo-v2.5-free from addendum #2's third emergency fill) via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping CONFIRMED ultimate health AND exposed the monitor's blind spot: **mimo-v2.5-free returned HTTP 429 (rate-limited/throttled) while the wire hadn't caught it** — the monitor's ping only distinguishes reachable (200) vs unreachable (000/502/5xx), NOT throttled (429). A 429-returning model is a model that will fail the moment it's actually used as fallback. Root rule (user standing preference): on the FIRST 429 do NOT retry the throttled model — switch fallback immediately. nemotron-3-ultra-free and nemotron-3.5-lightning-free both 200 (genuinely healthy — recovered from #2's HTTP 000 / Nvidia flakiness); deepseek-v4-flash-free still 400 (dead). Manual fallback-health probe pattern (curl per model, with mandatory `x-opencode-session` header since the MissingSessionID enforcement — bare pings return 400, per #1): `key=$(grep OPENCODE_GO_API_KEY ~/.hermes/.env | cut -d= -f2); for m in <model>; do curl -s -o /dev/null -w "%{http_code}" --max-time 35 -H "Authorization: Bearer $key" -H "Content-Type: application/json" -H "x-opencode-session: sess-XXX" -d "{\"model\":\"$m\",\"messages\":[{\"role\":\"user\",\"content\":\"hi\"}],\"max_tokens\":5}" https://opencode.ai/zen/v1/chat/completions; done`. Interpret: 200 healthy; 429 throttled — NEVER wire, switch at once; 400 model dead; 000 unreachable; 502 masked-edge (verify a real completion, not status). Known-good config.yaml re-synced by hand (patch tool). Overview of the week: the nemotron ↔ mimo emergency-fill flip-flop continued — the free-tier fallback is a self-correcting equilibrium, not a fault; NET EVENT worth watching: mimo's 429 shows the preference-order leader recovery can land on a model that is available-but-throttled, which the monitor alone won't catch.
|- **2026-09-07 addendum #2 (adaptive monitor)**: fallback flipped `ALL-DEAD` (prev tick) → **`mimo-v2.5-free`** (third emergency fill — pattern of 09-02 #1 / 09-06 #1; prior wired model was nemotron-3.5-lightning-free from addendum #1's re-wire) via `hermes config set fallback_providers.0.model mimo-v2.5-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping (with `x-opencode-session: sess-adaptive-monitor` header — mandatory since the MissingSessionID gateway enforcement; bare pings return 400, per #1): mimo-v2.5-free **HTTP 200 real completion** (finish_reason length, reasoning content present — NO masked 502, genuinely healthy); nemotron-3.5-lightning-free **HTTP 000** and nemotron-3-ultra-free **HTTP 000** (timeouts/drops — both Nvidia-backed down again, same 09-02/09-06 signature). Monitor pick corroborated. Monitor diff showed ONLY the fallback= line changed (free7/paid35/zen69 identical, no new models) → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool — was stale at nemotron-3.5-lightning-free). Nvidia-backed nemotrons remain flaky; mimo-v2.5-free is the effective preference-order leader; expect a flip back when a nemotron recovers.
|- **2026-09-07 addendum #4 (adaptive monitor, duplicate-trigger re-verification)**: re-triggered for the SAME flip as #3 (mimo-v2.5-free → `nemotron-3-ultra-free`) — config was ALREADY wired to ultra from #3's run (verified via both `hermes config get fallback_providers` and live ~/.hermes/config.yaml lines 13-17; NO config change this run). INDEPENDENT re-ping lesson re-confirmed: my first ≤30s curls returned false HTTP 000 for both nemotrons (the documented 29-34s Nvidia slow-edge, cf. #1's PING_TIMEOUT 8→30 fix) — the edge needs a ≥45s window to separate slow-but-healthy from hard-down. With `-m 45`: **nemotron-3-ultra-free HTTP 200 real completion @24.4s** (genuinely healthy — no masked 502), **nemotron-3.5-lightning-free HTTP 200 @32.1s**, **mimo-v2.5-free HTTP 429 FreeUsageLimitError** (throttle PERSISTS since #3 — correctly unwired; do NOT re-wire mimo until a 200 appears, per the never-retry-a-throttled-model rule). Monitor pick corroborated; ultra stays wired. Catalogs unchanged (free7/paid35/zen69) → no GO_RATES work. System converged; nothing new for the user.
|- **2026-09-08 addendum (adaptive monitor)**: fallback flipped `nemotron-3-ultra-free` → **`mimo-v2.5-free`** (FOURTH emergency fill — pattern of 09-02 #1 / 09-06 #1 / 09-07 #2; prior wired model was ultra from 09-07 #3/#4) via `hermes config set fallback_providers.0.model mimo-v2.5-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping (with `x-opencode-session: sess-adaptive-monitor` header — mandatory since MissingSessionID enforcement; bare pings 400): **nemotron-3-ultra-free HTTP 000 and nemotron-3.5-lightning-free HTTP 000 at -m 45** — BOTH Nvidia-backed leaders hard-down this tick (even the 45s window failed; harsher than the 09-07 #2 30s-window timeouts, and completely different from #4's slow-but-healthy 24-32s completions — the Nvidia free edge is in a rough patch); deepseek-v4-flash-free still 400 (dead); **mimo-v2.5-free HTTP 200 real completion @3s** (finish_reason length, 248 prompt/5 completion, 192 cached, cost \"0\" — NO masked 502, genuinely healthy; RECOVERED from the 09-07 #3/#4 429 throttle per the never-retry rule — waited for a 200, it arrived, now re-wirable), ling-3.0-flash-fin-free 200 (healthy, not in preference order → no switch). Monitor pick corroborated → wired mimo. Catalogs identical (free7/paid35/zen69 — no new models) → no GO_RATES work; both report scripts exit 0, no unknown-model warnings. Known-good config.yaml already at mimo-v2.5-free (from 09-07 #2's sync) → live config and known-good now in sync, no patch needed. Ultra/lightning remain Nvidia-flaky; mimo-v2.5-free is the effective preference-order leader until a nemotron recovers.
- **2026-09-08 addendum #2 (adaptive monitor, duplicate-trigger re-verification)**: re-triggered on the same event as #1 — diff showed `fallback=` UNCHANGED (mimo-v2.5-free) with `configured=` flipping nemotron-3-ultra-free → mimo-v2.5-free and `needfix=yes → no` (the #1 wiring landed; this is the confirmation tick). Verified via `hermes config get fallback_providers`: provider opencode-go, model **mimo-v2.5-free**, base_url zen/v1, key_env OPENCODE_GO_API_KEY (second local gemma entry untouched) → NO config change. Catalogs identical (free7/paid35/zen69 — no new/removed models) → no GO_RATES work; both report scripts exit 0 per #1. Known-good config.yaml already at mimo-v2.5-free (synced in #1). System converged cleanly; nothing new for the user.
- **2026-09-08 addendum #3 (adaptive monitor)**: fallback flipped `mimo-v2.5-free` → **`nemotron-3-ultra-free`** (preference-order leader recovered — the flip predicted in #1) via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping (with `x-opencode-session: sess-adaptive-monitor` header; --max-time 50): **nemotron-3-ultra-free HTTP 200 real 5/5-token completion** (finish_reason length, content present — NO masked 502, genuinely healthy); nemotron-3.5-lightning-free also 200/healthy; **mimo-v2.5-free HTTP 429 FreeUsageLimitError (throttled AGAIN — was 200 @3s this morning per #1; cycled back into rate-limit)** → per the never-retry-a-throttled-model rule, the flip away from mimo was REQUIRED, not just preference recovery; do NOT re-wire mimo until a 200 appears. Monitor diff showed ONLY the fallback= line changed (free7/paid35/zen69 identical — no new/removed models) → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool — stale at mimo-v2.5-free). Ultra ↔ lightning leader trade continues; ultra wired, mimo unwired (throttled).
- **2026-09-08 addendum #4 (adaptive monitor)**: fallback flipped `nemotron-3-ultra-free` → **`nemotron-3.5-lightning-free`** via `hermes config set fallback_providers.0.model nemotron-3.5-lightning-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping (with `x-opencode-session: sess-adaptive-monitor` header; --max-time 50): **nemotron-3-ultra-free HTTP 200 but masked Nvidia [502] \"Service temporarily overloaded\"** (DEAD — the 08-28 #2 signature; ultra keeps cycling 200-healthy ↔ masked-502/000 every few hours); **nemotron-3.5-lightning-free HTTP 200 real 5/5-token completion** (finish_reason length, content present \"Here's a thinking process\" — NO masked 502, genuinely healthy); mimo-v2.5-free also HTTP 200 real completion (healthy, still lower in preference order → no switch). Monitor pick corroborated → wired lightning. Monitor diff showed ONLY the fallback= line changed (free7/paid35/zen69 identical — no new/removed models) → no GO_RATES work, no new-model evaluation; both report scripts exit 0, no unknown-model warnings. Known-good config.yaml re-synced by hand (patch tool — was stale at nemotron-3-ultra-free from 09-08 #3). Ultra ↔ lightning leader trade continues; lightning wired, ultra dead this tick.
|- **2026-09-08 addendum #5 (adaptive monitor)**: fallback flipped `nemotron-3.5-lightning-free` → **`nemotron-3-ultra-free`** (preference-order leader recovered — the usual ultra ↔ lightning leader trade; prior wired model was lightning from #4) via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping (with `x-opencode-session: sess-adaptive-monitor` header; --max-time 50): **nemotron-3-ultra-free HTTP 200 real 5/5-token completion** (finish_reason length, content present — NO masked 502, genuinely healthy); **nemotron-3.5-lightning-free ALSO HTTP 200 real 5/5-token completion** (also genuinely healthy — this is the both-healthy leader trade, NOT a death-driven flip). Monitor pick corroborated → wired ultra; diff showed fallback= AND configured= swapped (lightning↔ultra) with needfix=no both ways (configured model healthy; CFAIL_THRESHOLD=2 never triggered). Catalogs identical (free7/paid35/zen69 — no new/removed models) → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool — was stale at nemotron-3.5-lightning-free from #4). Ultra ↔ lightning leader trade continues; ultra wired, both healthy.
- **2026-09-09 addendum (adaptive monitor)**: `fallback=ALL-DEAD` — CONFIRMED GENUINE this tick (TRUE negative, unlike the 09-01 #1 / 09-07 #1 false negatives); prior fallback was nemotron-3-ultra-free from 09-08 #5; diff showed configured= ALSO flipping nemotron-3.5-lightning-free → nemotron-3-ultra-free (the #5 wiring landing as a confirmation tick, same as 09-08 #2 pattern) with needfix=no (no healthy preference-order candidate → nothing to fix). Catalogs IDENTICAL to the 09-08 baseline (free7/paid35/zen69 — no new/removed models) → no GO_RATES work, no new-model evaluation. INDEPENDENT re-pings (with `x-opencode-session: sess-adaptive-monitor` header; --max-time 50; completion-verified): **nemotron-3-ultra-free HTTP 000 and nemotron-3.5-lightning-free HTTP 000** (BOTH Nvidia-backed leaders hard-down even at the 50s window — same rough patch as 09-08 #1, NOT #4's slow-but-healthy 24-32s completions); **deepseek-v4-flash-free HTTP 400 "Model is unavailable"** (dead, unchanged since 09-02); **mimo-v2.5-free HTTP 429 FreeUsageLimitError** (throttled — correctly unwired; go_fallback_state.json shows mimo banned (~20min cooldown) and ultra cfalls=2, corroborating the rough patch); **ling-3.0-flash-fin-free HTTP 200 real completion** (healthy — but NOT in preference order → no switch, x-preview-f-free precedent). This ALL-DEAD is a TRUE negative: all six preference-order models (ultra, hy3-gone, lightning, flash, mimo, laguna-gone) are genuinely unusable right now. NO config change: configured stays `nemotron-3-ultra-free` (last healthy preference-order pick; wiring mimo=retry-throttled and flash=dead are both pointless, ling is out-of-preference; expect a recovery flip when a nemotron returns). Known-good config.yaml NOT re-synced (no effective change, 09-01 #1 practice). Paid tier unaffected — fallback only matters post-bucket-exhaustion and meters were low per 09-04 #2.
|- **2026-09-09 addendum #2 (adaptive monitor, RECOVERY)**: fallback self-corrected `ALL-DEAD` → **`nemotron-3-ultra-free`** (preference-order leader recovered from the genuine all-dead of addendum #1; configured= already nemotron-3-ultra-free, needfix=no → NO config change, exactly the recovery flip predicted in #1). INDEPENDENT re-ping CONFIRMED genuine health (with `x-opencode-session: sess-cron-adaptive-monitor-check` header; --max-time 50): **nemotron-3-ultra-free HTTP 200 real 5/5-token completion** (finish_reason length, content present — NO masked 502) and **nemotron-3.5-lightning-free also HTTP 200 real 5/5 completion** (both Nvidia-backed leaders recovered together). Verified via `hermes config get fallback_providers` (provider opencode-go, model nemotron-3-ultra-free, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). Catalogs identical (free7/paid35/zen69 — no new/removed models) → no GO_RATES work, no new-model evaluation; both report scripts exit 0 per prior ticks. Known-good config.yaml already at nemotron-3-ultra-free (synced 09-08 #5) → no patch needed. Ultra ↔ lightning leader trade resumes; system converged.
|- **2026-09-09 addendum #3 (adaptive monitor)**: fallback flipped `nemotron-3-ultra-free` → **`nemotron-3.5-lightning-free`** (usual ultra ↔ lightning leader trade; prior wired model was ultra from 09-09 #2) via `hermes config set fallback_providers.0.model nemotron-3.5-lightning-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, model nemotron-3.5-lightning-free, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping (with `x-opencode-session: sess-cron-adaptive-monitor-check` header; --max-time 50): **nemotron-3-ultra-free HTTP 000 even at the 50s window — hard down again (Nvidia-backed rough patch, cf. 09-08 #1/#4, 09-09 #1: harsher than #2's slow-but-healthy completions)**; **nemotron-3.5-lightning-free HTTP 200 real 5/5-token completion** (finish_reason length, content present — NO masked 502, genuinely healthy). Monitor pick corroborated → wired lightning. Note the FIRST pings without the mandatory `x-opencode-session` header returned HTTP 400 MissingSessionID ("free tier can only be used in OpenCode") — re-ran with the session header; bare-ping 400 is NOT a health signal (cf. 09-07 #1). Monitor diff showed ONLY the fallback= line changed (free7/paid35/zen69 identical — no new/removed models) → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool — was stale at nemotron-3-ultra-free from 09-08 #5/09-09 #2). Ultra ↔ lightning leader trade continues; lightning wired, ultra hard-down this tick.
|- **2026-09-11 addendum (adaptive monitor)**: tick went `fetch=FAIL` (prev) → full healthy snapshot (fallback=mimo-v2.5-free, configured=nemotron-3.5-lightning-free, needfix=no — cfalls=1, threshold 2). Prior configured model was lightning from 09-09 #3. INDEPENDENT re-pings (with `x-opencode-session` header; --max-time 50): **nemotron-3.5-lightning-free EMPTY response** (no completion within 50s — down/slow, corroborates the monitor's demotion); **mimo-v2.5-free HTTP 200 real 5-token completion** (finish_reason length — genuinely healthy; state file shows no ban; recovered from the 09-08 #3 429, waited for a 200 as the rule requires). Monitor pick corroborated → wired via `hermes config set fallback_providers.0.model mimo-v2.5-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, model mimo-v2.5-free, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). FIFTH emergency fill to mimo (09-02 #1 / 09-06 #1 / 09-07 #2 / 09-08 #1 pattern) — lightning unresponsive this tick, mimo healthy. CATALOG: paid35→paid37 added **`deepseek-flash`** + **`deepseek-v4.1-flash`** (sole catalog diffs; free7/zen69 unchanged) → priced into BOTH `token_usage_report.py` + `morning_context.py` as `# GUESS ≈ deepseek-v4-flash` tier (0.14, 0.28, 0.0028, 0.0) — no official rates expected (go.mdx 404; deepseek v4 family now peak/off-peak tiered per 09-04 #1 note). Both scripts exit 0, no unknown-model warnings (meter 5h 1.8% / 7d 7.5% / 30d 21.3%). Known-good config.yaml re-synced by hand (patch tool — was stale at lightning from 09-09 #3). Expect a flip back when a nemotron recovers; ultra ↔ lightning leader trade continues, both Nvidia-backed down/unresponsive this tick.
- **2026-09-11 addendum #2 (adaptive monitor)**: fallback flipped `mimo-v2.5-free` → **`nemotron-3-ultra-free`** (preference-order leader recovered; configured= line landed at mimo-v2.5-free, the #1 wiring's confirmation tick, 09-08 #2 pattern) via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, model nemotron-3-ultra-free, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping (with `x-opencode-session: sess-cron-adaptive-monitor-check` header; --max-time 50): **nemotron-3-ultra-free HTTP 200 real 5/5-token completion** (finish_reason length, content present — NO masked 502, genuinely healthy); **nemotron-3.5-lightning-free HTTP 000** (no response within 50s — Nvidia-backed down/slow again); **mimo-v2.5-free HTTP 429 FreeUsageLimitError** (throttled AGAIN — cycled into rate-limit ~2h after #1's healthy 200; per the never-retry-a-throttled-model rule, flipping off mimo was REQUIRED, not merely preference recovery). Monitor diff showed ONLY the fallback= line changed (free7/paid37/zen69 identical — no new/removed models; deepseek-flash/deepseek-v4.1-flash already priced in #1) → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool — was stale at mimo-v2.5-free). Ultra ↔ lightning leader trade continues; ultra wired, mimo unwired (throttled).
- **2026-09-13 addendum (adaptive monitor)**: fallback flipped `nemotron-3-ultra-free` → **`mimo-v2.5-free`** (SIXTH emergency fill — pattern of 09-02 #1 / 09-06 #1 / 09-07 #2 / 09-08 #1 / 09-11 #1; prior wired model was ultra from 09-11 #2) via `hermes config set fallback_providers.0.model mimo-v2.5-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, model mimo-v2.5-free, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping (with `x-opencode-session: sess-cron-adaptive-monitor-check` header; --max-time 50): **nemotron-3-ultra-free HTTP 200 real 5-token completion** (genuinely healthy on the independent ping, but the monitor's 2×30s snapshot demoted it — the known Nvidia slow-edge profile; its cfalls was below the CFAIL_THRESHOLD=2, hence needfix=no); **nemotron-3.5-lightning-free HTTP 000** (no response within 50s — down); **deepseek-v4-flash-free HTTP 400 "Model is unavailable"** (dead, unchanged since 09-02); **mimo-v2.5-free HTTP 200 real completion @fast** (248 prompt/5 completion, 192 cached — NO masked 502, NO 429: recovered from the 09-11 #2 throttle per the never-retry rule). Monitor pick corroborated → wired mimo. Monitor diff showed ONLY the fallback= line changed (free7/paid37/zen69 identical — no new/removed models) → no GO_RATES work, no new-model evaluation; both report scripts exit 0 per prior ticks. Known-good config.yaml re-synced by hand (patch tool — was stale at nemotron-3-ultra-free from 09-11 #2). Ultra/lightning remain Nvidia-flaky (ultra's health contested this tick: independent 200 vs monitor demotion — slow-edge); mimo-v2.5-free is the effective preference-order leader until a nemotron shows a stable fast completion.
- **2026-09-13 addendum #3 (adaptive monitor)**: fallback flipped `mimo-v2.5-free` → **`nemotron-3-ultra-free`** (preference-order leader recovered — the flip predicted in #2; prior wired model was mimo from #1) via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, model nemotron-3-ultra-free, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping (with `x-opencode-session: sess-cron-adaptive-monitor-check` header; --max-time 50): **nemotron-3-ultra-free HTTP 200 real 5-token completion** (finish_reason length, 17 prompt/5 completion — NO masked 502, genuinely healthy); **mimo-v2.5-free HTTP 429 FreeUsageLimitError** (throttled — ~2h after #1's healthy 200; state file `go_fallback_state.json` now shows `bans: {mimo-v2.5-free: ...}`; per the never-retry-a-throttled-model rule the flip off mimo was REQUIRED, not preference-only); **nemotron-3.5-lightning-free HTTP 000** (no response in 50s — Nvidia-backed down). Monitor's `needfix=no` explained: all cfalls=1 < CFAIL_THRESHOLD=2 (mimo's 429 read is new; the monitor's 2-consecutive-tick rule hadn't fired yet) — the independent 429 caught it first; monitor pick corroborated regardless. Monitor diff showed ONLY the fallback= line changed (free7/paid37/zen69 identical — no new/removed models) → no GO_RATES work, no new-model evaluation; both report scripts exit 0 per prior ticks. Known-good config.yaml re-synced by hand (patch tool — was stale at mimo-v2.5-free). SEVENTH emergency-fill reversal (09-02 #1 / 09-06 #1 / 09-07 #2 / 09-08 #1 / 09-11 #1 / 09-13 #1 pattern); ultra wired, mimo unwired (throttled/ban), lightning down. Expect further ultra ↔ lightning/mimo trades while the Nvidia-backed edge stays flaky.
- **2026-09-14 addendum (adaptive monitor, confirmation tick)**: `fallback=` UNCHANGED at **`nemotron-3-ultra-free`** (healthy, top of preference order, still in free=); `configured=` caught up mimo-v2.5-free → nemotron-3-ultra-free (the 09-13 #3 wiring landing — same confirmation pattern as 09-08 #2 / 09-09 #1), `needfix=no`. Verified via `hermes config get fallback_providers`: provider opencode-go, model nemotron-3-ultra-free, base_url zen/v1, key_env OPENCODE_GO_API_KEY (second local gemma entry untouched) → NO config change this tick. Catalogs identical (free7/paid37/zen69 — no new/removed models) → no GO_RATES work, no new-model evaluation. System converged cleanly; nothing new for the user.
- **2026-09-15 addendum (adaptive monitor)**: fallback flipped `nemotron-3-ultra-free` → **`nemotron-3.5-lightning-free`** (usual ultra ↔ lightning leader trade; prior wired model was ultra from 09-13 #3) via `hermes config set fallback_providers.0.model nemotron-3.5-lightning-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, model nemotron-3.5-lightning-free, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-ping (with `x-opencode-session` header; --max-time 45): **nemotron-3-ultra-free HTTP 000 twice (45s/40s — no response at all, hard-down signature, cf. 09-05/09-08 #1)**; **nemotron-3.5-lightning-free HTTP 200 real 5-token completion @0.64s** (finish_reason length, content present — NO masked 502, genuinely healthy). Monitor pick corroborated → wired lightning; `needfix=no` explained: ultra cfalls=1 < CFAIL_THRESHOLD=2 (first bad read this tick; the monitor's twice-consecutive rule hadn't fired yet — the independent hard-down caught it first). Monitor diff showed ONLY the fallback= line changed (free7/paid37/zen69 identical — no new/removed models) → no GO_RATES work, no new-model evaluation; both report scripts exit 0, no unknown-model warnings. Known-good config.yaml re-synced by hand (patch tool — was stale at nemotron-3-ultra-free). Ultra ↔ lightning leader trade continues; lightning wired, ultra hard-down this tick.
- **2026-09-15 addendum #2 (adaptive monitor)**: fallback flipped back to **`nemotron-3-ultra-free`** (preference-order leader recovered; prior wired model lightning from addendum #1) via `hermes config set fallback_providers.0.model nemotron-3-ultra-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, model nemotron-3-ultra-free, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-pings (with `x-opencode-session` header; --max-time 50): FIRST ultra ping hit the transient masked-502 (`[502] Upstream error from Nvidia: Service temporarily overloaded` — the 08-28 #2 signature), but a RECHECK seconds later returned **HTTP 200 real 5/5-token completion** (finish_reason length, content present, cost "0" — genuinely healthy; masked-502 was transient, flips between pings); lightning independently verified healthy earlier this tick (200 real 5/5 @0.63s). Both-healthy leader trade → monitor pick (ultra, preference-order leader) corroborated → wired ultra. Monitor diff showed ONLY the fallback=/configured= swap (free7/paid37/zen69 identical — no new/removed models) → no GO_RATES work, no new-model evaluation. Known-good config.yaml re-synced by hand (patch tool — was stale at nemotron-3.5-lightning-free). Ultra ↔ lightning leader trade continues; ultra wired, both healthy.
- **2026-09-18 addendum (autonomy run — monitor job failed mid-action)**: 19:32 tick detected `paid37→paid38` / `zen69→zen70` = ONE new paid model **`union-alpha`** (fallback/config/free unchanged — ultra still wired, needfix=no) but the agent run died with `RuntimeError: Connection error` (its own LLM call failed; transient) before acting. Completed its intended GO_RATES action by hand: **no official rate anywhere** — anomalycha/opencode repo raw path 404s (as since 08-18), live opencode.ai/docs/go SPA table has no union row, zen/v1/models catalog omits it → priced `union-alpha` ≈ `omen-alpha` `(0.20, 0.66, 0.04, 0.0)` marked `# GUESS` in BOTH `token_usage_report.py` and `morning_context.py`; both scripts exit 0, **no unknown-model warnings**. Next tick will re-see the stale diff (failed run never updated previous-hash) and should confirm convergence. Quota report healthy (5h 0.2% / 7d 1.5% / 30d 13.7%).
- **2026-09-18 addendum #2 (adaptive monitor)**: `fallback=ALL-DEAD` — CONFIRMED GENUINE again (pattern of 09-09 #1 true negative; prior fallback nemotron-3-ultra-free from 09-15 #2), but with a **NEW root cause: upstream free-tier gating change**. Independent pings (with the mandatory `x-opencode-session` header; browser UA; --max-time 50) — **every one of the 7 `-free` models on zen/v1 now returns HTTP 403 `FreeTierError` \"OpenCode's free tier can only be used from within OpenCode\"**. This is NOT the 09-07 #1 MissingSessionID 400 (which a session header fixed): the 403 persists across THREE session shapes — the monitor's own `sess-adaptive-monitor`, this cron's `sess-cron-adaptive-monitor-check`, AND a real session id from state.db (`cron_81a8bc241220_20260918_200005`). The free tier has switched from session-header enforcement to origin/console gating: external API callers can no longer reach it at all. Paid tier UNAFFECTED and verified healthy: `deepseek-v4-flash` on zen/go/v1 with session → **HTTP 200 real completion**. CONFIG: `configured=nemotron-3-ultra-free`, `needfix=no` — verified via `hermes config get fallback_providers` (provider opencode-go, model nemotron-3-ultra-free, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched) → **NO config change** (no healthy free candidate exists to switch to; keep last healthy preference-order pick per 09-09 #1 practice; wiring anything would be a pointless rewrite). CATALOG: paid38→paid37 / zen70→zen69 = **`union-alpha` REMOVED** from both listings (the 09-18 #1 priced model — flip-flop: added 19:32, gone by this 20:00 tick; its orphaned GO_RATES GUESS entry is harmless and left in place per 08-20 muse-spark-1.2 precedent). No new paid/free models → no pricing work; both scripts exit 0, no unknown-model warnings. ALL-DEAD means the free-tier escape hatch is CLOSED until upstream reverses the gating or inside-OpenCode sessions are used — for dollar-bucket exhaustion the only fallback left is the local gemma (custom provider) entry.

- **2026-09-18 addendum #3 (adaptive monitor, late)**: tick added **`jev-1.13-free`** to free= (free7→free8) and `jev-1.13`/`jev-1.13-free`/`deepseek-v4.1-flash`/`qwen3.8-flash` to zen listing (zen69→zen73); paid37 UNCHANGED (deepseek-v4.1-flash & qwen3.8-flash already priced: 09-11 #1 / 08-28 #1; jev-1.13 is zen-only, not in paidN → no GO_RATES work, gpt-6-astra precedent). Fallback STILL **ALL-DEAD** (2nd consecutive tick). INDEPENDENT re-probes (x-opencode-session + browser UA + 50s window): 403 `FreeTierError` console-gating for nemotron-3-ultra-free / nemotron-3.5-lightning-free / mimo-v2.5-free / ling-3.0-flash-fin-free (09-18 #2 gating persists); deepseek-v4-flash-free 400 "Model is unavailable"; NEW jev-1.13-free 500 (dead on arrival, not in preference order anyway); muse-spark-1.2/1.3-contributor-free 500. PAID sanity: deepseek-v4-flash on zen/go/v1 **HTTP 200 real completion** — paid tier unaffected. CONFIG: unchanged — verified via `hermes config get fallback_providers` (opencode-go / nemotron-3-ultra-free / zen/v1 / OPENCODE_GO_API_KEY; second local gemma entry untouched); no healthy candidate → keep last healthy preference pick per 09-09 #1 practice, needfix=no. Both report scripts exit 0, no unknown-model warnings. Free-tier escape hatch remains CLOSED (upstream console gating); paid tier is the only working path until upstream reverses it.
- **2026-09-19 addendum (FULL RATE RE-SYNC — the pass deferred by 09-04 #1)**: user supplied the live rendered pricing table (page header "Last updated: Sep 19, 2026"). Replaced `GO_RATES` in BOTH `token_usage_report.py` + `morning_context.py` (backups `.bak-20260919-174458`), added `GO_MODEL_MONTHLY` + `DS_PEAK_TIERS`. **Every 09-04 #1 drift item is now fixed**: `glm-5.3-flash` 1.40/4.40/0.26 → **0.15/0.50/0.03** (9.3x overcount on input — it is a genuinely cheap $60/mo lane, not a GLM-tier clone), `qwen3.8-flash` 2.00/6.00/0.25/2.50 → **0.15/0.47/0.016/0.20** (13x overcount), `longcat-2.0` → **0.30/1.20/0.006** (4.7x over), `muse-spark-1.2/1.3-contributor` → **0.10/0.20/0.002** (5x over), `hy4-preview` 0.14/0.58/0.035 → **0.834/2.501/0.042** (~6x UNDER), `kimi-k2.6` cache-read → 0.16, `minimax-m2.5/m2.7` cache-write → 0.375, `grok-4.6` cache-read → 0.50. **Durable lesson: the meter's error direction is not uniform** — stale GUESS entries understated some models (hy4-preview, all DeepSeek) and overstated others by up to 13x, so "it reads low" is not a safe heuristic; only a line-by-line diff against the live table settles it.
- **2026-09-19 addendum #2 (DeepSeek peak/off-peak + per-model caps)**: DeepSeek models now bill two tiers — peak = **01:00-04:00 + 06:00-10:00 UTC, Mon-Fri** (7h/day = 20.8% of wall clock); all other hours, weekends, off-peak. Off-peak v4-flash 0.15/0.60/0.003, peak 0.30/1.20/0.006; off-peak v4-pro 0.66/1.98/0.022, peak 1.32/3.96/0.044. `session_model_usage` stores only `first_seen`/`last_seen` **aggregates per (session, model, ...)** — no per-call timestamps, so retro-tiering is impossible; GO_RATES therefore carries a **time-weighted blend** (0.7917 off-peak + 0.2083 peak: v4-flash 0.18125/0.725/0.003625, v4-pro 0.7975/2.3925/0.026583) with the exact pairs kept in `DS_PEAK_TIERS` for a future tiered implementation. **The old single pool is gone**: docs now define limits per MODEL (`GO_MODEL_MONTHLY`), split 20% / 5h, 50% / 7d, 100% / 30d — the familiar $12/$30/$60 triple is just the docs' *example* for a $60-tier model, so a global-$60 denominator misstates risk whenever usage is concentrated on a cheaper-cap model (real case: 30d flash = 53.5% of its own $30 cap but only 27.2% of the legacy $60 pool). Per-model caps stay UNWIRED pending explicit approval; the bucket math is the last piece of this re-sync.
- **2026-09-19 addendum #3 (PITFALL — regenerated rate tuple silently mis-slotted)**: generating the new table with `f2, f1, p2, p1 = BLENDED[...] + (0.0,)` unpacked a 3-tuple into 4 names, so the flash entries were written as `(in, out=0.003625, cache_read=0.725)` — the **output** rate landed in the cache-read slot. Because flash carries 233M cached tokens/30d, the meter then read **$177.50 / 295.8% for 30d** against a true **$16.31 / 27.2%** (10.9x inflation), and the "NEAR CAP: switch heavy work to flash" verdict was pure artifact. **Rule: after regenerating any rate table, hand-recompute one window from raw column sums (`SELECT SUM(input_tokens), SUM(output_tokens), SUM(cache_read_tokens) ...`) and require an exact match before trusting the meter** — a 4-tuple rate dict cannot be validated by "script exits 0, no unknown-model warnings", which passed both before and after the bug. Both scripts re-verified: 5h 10.6% / 7d 9.2% / 30d 27.2% of the legacy pool, recomputed flash 5h $1.0005 hand-matched to the meter's $1.0005.
- **2026-09-19 addendum #4 (forward risk)**: **`deepseek-v4.1-flash` 4x promo ENDS 2026-09-20** — monthly limit $60 → $15 and req/5h 26,000 → 6,500. This is the desktop's active driver (localStorage `hermes.desktop.model-presets`), so its lane allowance drops 4x tomorrow while measured usage there is still tiny ($0.27/30d) — no action needed, but the fallback/desktop pick should be revisited then. Related: Kimi K3 ($15/mo, **110 req/5h**) and glm-5.3 ($15/mo, 220 req/5h) are the scarcest lanes in request terms; a single kanban worker card burns 40-60 calls, so ONE card can consume ~half of K3's 5h request budget — request count, not dollars, is the binding constraint on those lanes.
- **2026-09-22 addendum (adaptive monitor)**: free tier still **ALL-DEAD** (3rd consecutive tick; upstream console-gating persists — every `-free` model returned HTTP 403 `FreeTierError` on independent re-probes with x-opencode-session + browser UA + 50s window: nemotron-3-ultra-free / nemotron-3.5-lightning-free / mimo-v2.5-free / mimo-v2.6-flash-free / ling-3.0-flash-fin-free / muse-spark-1.2/1.3-contributor-free; jev-1.13-free 500 (dead on arrival per 09-18 #3); deepseek-v4-flash-free 400 "Model is unavailable" AND now ABSENT from zen/v1 catalog — removed upstream along with hy3-free/laguna-s-2.1-free; free= line swapped it for NEW **mimo-v2.6-flash-free**, which is equally gated). PAID sanity: **deepseek-v4-flash on zen/go/v1 HTTP 200 real completion** — paid tier unaffected. CONFIG: unchanged — `configured=nemotron-3-ultra-free`, `needfix=no`; no healthy free candidate, keep last healthy preference pick per 09-09 #1 practice (NO `hermes config set` this tick). CATALOG: paid37→**paid33** / zen73→**zen78** vs 09-18 #3 snapshot. THREE new paid models priced this tick from the LIVE opencode.ai/docs/go page (2026-09-22 fetch — all VERIFIED, no GUESS): **`mimo-v2.6-flash` (0.14, 0.28, 0.0028, 0.0)** $60/mo 30,100 req/5h (≈ v2.5 sibling); **`mimo-v2.6-pro` (0.435, 0.87, 0.003625, 0.0)** $15/mo 3,250 req/5h (≈ v2.5-pro); **`grok-4.7` (2.00, 6.00, 0.50, 0.0)** ≤200K tier ($15/mo, **169 req/5h** — scarcest lane in the catalog), >200K tier 4.00/12.00/1.00 recorded here but NOT in GO_RATES (no cached-token usage over 200K observed; grok-4.6 precedent keeps only the ≤200K tuple). All three added to `GO_RATES` × 2 scripts + `GO_MODEL_MONTHLY` × 2 + `PER_MODEL_REQ_CAPS` (token_usage_report.py only — morning_context.py has no req-cap table). Scripts verified: both exit 0, no unknown-model warnings; quota clean — 5h 2.5% / 7d 8.2% / 30d 24.4% legacy pool, flash 47.6% of its $30 cap is nearest ceiling. Forward-risk check (09-19 #4): deepseek-v4.1-flash post-promo, usage still tiny ($0.39/30d, 0.6% of its lane) — desktop pick revisit still non-urgent. NOTE for next ticks: paid33/zen78 divergence is now structural (zen-only: claude/gemini/gpt-codex families, grok-4.5, grok-build-0.1, test, test-novita-dsf4.1, all `-free`; paid-only: omen-alpha — flip-flop #3: READDED — hy3, hy4-preview, longcat-2.0, qwen3.7/3.8 variants).

- **2026-09-23 addendum (adaptive monitor)**: FIRST concrete free-tier recovery since the 09-18 #2 console-gating — fallback flipped away from `ALL-DEAD` to **`space-bunny-free`** (preference-order list didn't contain it, so this was an out-of-list pick via rule 4-style evaluation; prior wired model nemotron-3-ultra-free from 09-15 #2 stayed configured-but-dead all through the 09-18/09-19/09-22 ALL-DEAD ticks) via `hermes config set fallback_providers.0.model space-bunny-free` (index-qualified form; plain form still crashes, see 08-21 #2), verified via `hermes config get fallback_providers` (provider opencode-go, model **space-bunny-free**, base_url zen/v1, key_env OPENCODE_GO_API_KEY; second local gemma entry untouched). INDEPENDENT re-probes (x-opencode-session + browser UA + 50s window): **space-bunny-free HTTP 200 real completion** — the ONLY healthy *-free model on zen/v1 this tick; all other 8 free models still HTTP 403 `FreeTierError` (09-18 gating persists: nemotron-3-ultra-free / nemotron-3.5-lightning-free / mimo-v2.5-free / mimo-v2.6-flash-free / ling-3.0-flash-fin-free / muse-spark-1.2/1.3-contributor-free / jev-1.13-free). CATALOG: free8→**free9** (+space-bunny-free), paid33→**paid35** (+**gpt-6-luna**, +**space-bunny-free** — the promo free model ALSO shows on the paid/go catalog), zen78→**zen79** (+space-bunny-free). PRICING (09-23 live docs): **`gpt-6-luna` (0.10, 0.50, 0.01, 0.125)** ≤272K tier, $15/mo, VERIFIED — >272K tier 0.20/0.75/0.02 recorded in comment only, NOT in GO_RATES (gpt-5.6-luna ≤272K precedent); **`space-bunny-free` (0.0, 0.0, 0.0, 0.0)** free (limited time), VERIFIED — added to GO_RATES × 2 scripts + GO_MODEL_MONTHLY × 2 (gpt-6-luna: 15). PER_MODEL_REQ_CAPS: gpt-6-luna NOT added (req-cap not captured; 09-22 addendum pattern adds only verified caps). Known-good config.yaml re-synced by hand (patch tool — was stale at nemotron-3-ultra-free, dating to 09-15 #2). Scripts verified: both exit 0, no unknown-model warnings; quota clean — 5h 2.4% / 7d 9.4% / 30d 21.8% legacy pool, flash 42.3% of its $30 cap nearest ceiling. Forward note: space-bunny-free is promo/"limited time" — the escape hatch may vanish as abruptly as it appeared (union-alpha 09-18 flip-flop precedent); monitor needs a preference-order entry if it persists.
- **2026-09-25 addendum (adaptive monitor)**: zen79→**zen80** sole diff — **`qwen3.8-max`** added to full zen/v1 listing (already in paid35 with GO_RATES entry in both scripts from the paid catalog — NO pricing work; verified `(2.00, 6.00, 0.25, 2.50)` $15/mo present in token_usage_report.py + morning_context.py; both exit 0, no unknown-model warnings). Fallback/config UNCHANGED: monitor still prints `fallback=ALL-DEAD` (monitor-limited — space-bunny-free is out-of-list in FALLBACK_ORDER, the documented 09-23/09-24 state) while the wired escape hatch **space-bunny-free** remains healthy: independent probe (x-opencode-session + browser UA, 50s) **HTTP 200 real completion** (144 prompt tokens, finish_reason length). All other 8 *-free models still HTTP 403 `FreeTierError` console-gating (nemotron-3-ultra-free / nemotron-3.5-lightning-free / mimo-v2.5-free re-verified this tick; jev-1.13-free 500 dead on arrival per 09-18 #3). Quota clean: 5h 0.1% / 7d 10.9% / 30d 22.6% legacy pool; per-model flash 43.8% of its $30 cap nearest ceiling. No config change, no needfix.
- **2026-09-24 addendum (adaptive monitor, confirmation tick)**: `configured=` caught up nemotron-3-ultra-free → **`space-bunny-free`** (the 09-23 wiring landing — same confirmation pattern as 09-14; provider opencode-go, zen/v1, OPENCODE_GO_API_KEY, NO config change this tick). `fallback=ALL-DEAD` persists but is MONITOR-LIMITED, not a real all-dead: space-bunny-free is out-of-list in adaptive_monitor.py `FALLBACK_ORDER`, so the monitor can never report it as fallback. INDEPENDENT re-probe (x-opencode-session + browser UA, 45s window): **space-bunny-free HTTP 200 real 5-token completion** — still the ONLY healthy *-free model on zen/v1; all other 8 free models HTTP 403 `FreeTierError` (09-18 console-gating persists: nemotron-3-ultra-free / nemotron-3.5-lightning-free / mimo-v2.5-free / mimo-v2.6-flash-free / jev-1.13-free / ling-3.0-flash-fin-free / muse-spark-1.2-contributor-free / muse-spark-1.3-contributor-free). CATALOG: identical (free9/paid35/zen79) → no GO_RATES work, no new-model evaluation. Scripts verified: both exit 0, no unknown-model warnings; quota clean — 5h 0.2% / 7d 9.9% / 30d 22.0% legacy pool, flash 42.8% of its $30 cap nearest ceiling (its own lane). `FALLBACK_ORDER` edit still deferred (promo/limited-time model may vanish like union-alpha 09-18) — expected behavior: monitor keeps printing ALL-DEAD while the wired bunny escape hatch actually works; only a real bunny death would then close the hatch silently, so the preference-order entry remains the open follow-up.
- **2026-09-26 addendum (adaptive monitor)**: free10→**free11** / paid35→**paid36** / zen80→**zen81** = ONE new model **`longcat-2.5-preview-free`** added to all three catalog lines (first new `-free` model since the 09-23 bunny; also shows on the zen/go paid catalog like space-bunny-free). Fallback/config UNCHANGED: `fallback=ALL-DEAD` (monitor-limited — space-bunny-free out-of-list in FALLBACK_ORDER, documented 09-23/09-24/09-25 state), `configured=space-bunny-free`, `needfix=no`. INDEPENDENT re-probes (x-opencode-session + browser UA, 50s window): **space-bunny-free HTTP 200 real completion — wired escape hatch still healthy**; **longcat-2.5-preview-free HTTP 403 `FreeTierError`** (console-gating persists — same signature as all 8 other gated free models, so no fallback switch; healthy-but-out-of-preference intuition doesn't apply, it's 403-gated); **nemotron-3-ultra-free HTTP 403** (gating persists, re-verified). PRICING: `longcat-2.5-preview-free (0.0, 0.0, 0.0, 0.0)` free preview — space-bunny-free precedent, added to `GO_RATES` × 2 scripts (token_usage_report.py L46, morning_context.py L70); NO `GO_MODEL_MONTHLY` entry (free model — no monthly plan; bunny precedent). GO_RATES verified in sync (search across both scripts, exact match at L46/L70). Scripts verified: both exit 0, no unknown-model warnings; quota clean — 5h 0.5% / 7d 10.5% / 30d 22.9% legacy pool; flash 44.5% of its $30 cap nearest ceiling (its own lane). No config change; escape hatch remains wired and healthy.
